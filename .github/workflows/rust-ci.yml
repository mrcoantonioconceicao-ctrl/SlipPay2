name: Rust CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Instalar Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    - name: Compilar Workspace Rust
      run: cargo build --workspace --verbose || cargo build --verbose

    - name: Rodar Testes
      run: cargo test --workspace --verbose || cargo test --verbose
